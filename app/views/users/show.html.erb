<h1><%= @user.name %>のページ</h1>
<p>メールアドレス: <%= @user.email %></p>
<p>
  <strong>画像:</strong>
  <%= image_tag @user.picture.url, style:'width:50px;height:50px'if @user.picture && @user.picture.url %>
</p>

<br>
<td><%= link_to 'Edit Profile', edit_user_path(@user.id) %>
<br><br><br>

<%= link_to '投稿', new_feed_path, class: "btn btn-info" %>
<%= link_to '投稿一覧', feeds_path %>

<%= link_to 'お気に入り画像一覧へ', favorites_path %>
